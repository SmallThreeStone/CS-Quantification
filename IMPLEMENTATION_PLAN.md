# CS2 饰品量化监控平台实施目标

## 一、项目定位

建设一个部署在自有云服务器上的 CS2 饰品量化监控平台，用于持续采集饰品市场数据、识别价格与买卖盘异动、生成告警推送，并为倒货过程中的买入、卖出、持有、观望提供数据参考。

平台定位为私有化决策辅助工具，不直接执行自动交易，不承诺预测收益。核心价值是把分散的市场数据整理成可追踪、可复盘、可解释的监控盘。

## 二、核心目标

1. 监控重点饰品的价格、在售数量、求购数量、成交量和波动情况。
2. 在饰品出现明显异动时，及时生成站内告警和外部推送。
3. 展示单个饰品的历史走势、买卖盘变化、历史告警和时间段热力图。
4. 通过规则评分输出买入机会、卖压风险、扫货拉升、流动性异常等参考状态。
   1. 实施进度：已增加数据可信度降权，低可信饰品会降低买入/卖出参考分，且最大降权可在策略配置中调整。
   2. 实施进度：已基于降权后买入/卖出分、告警方向和数据可信度输出买入、持有、卖出、观望参考信号。
5. 保存原始行情、告警和回测数据，支持后续复盘和策略调参。
6. 支持后续扩展多平台价差、手续费计算、净利润空间和更复杂的量化策略。

## 三、MVP 范围

第一阶段只做个人使用的私有监控盘，优先稳定和可复盘。

1. 监控范围：先接入 100-300 个重点饰品。
2. 数据源：先接入 1 个主要市场数据源，预留第二数据源扩展位。
3. 采集指标：底价、在售数量、最高求购价、求购数量、成交量、历史均价。
4. 告警类型：在售变化、求购变化、底价变化、成交量异常、价格波动异常。
   1. 实施进度：已增加成交量异常告警，并支持在策略配置中调整成交量变化率阈值。
   2. 实施进度：已增加价格波动异常告警，并支持在策略配置中调整价格波动率阈值。
5. 页面范围：监控总览、饰品详情、异动告警、机会榜、策略配置、数据源状态。
6. 推送范围：Telegram / 企业微信 / 邮件三选一，先实现一种稳定渠道。
   1. 实施进度：已增加数据源异常推送熔断，本轮采集真实字段占比过低或 fallback 覆盖过高时跳过外部推送。
7. 暂不实现：自动交易、公众注册、多用户权限、复杂 AI 预测、全市场无差别扫描。

## 四、产品计划

### 1. 监控池

建立不同监控池，降低无效采集和告警噪音。

1. 重点池：常倒货饰品，5-10 分钟采集一次。
2. 观察池：热门饰品、箱子、贴纸，15-30 分钟采集一次。
3. 事件池：版本更新、Major、贴纸打折期间临时加入，按需提高采集频率。
4. 实施进度：已建立重点池、观察池、事件池，worker 按池间隔采集到期饰品。
5. 实施进度：已增加监控池新增和编辑能力，可维护池名称、采集间隔和说明。

### 2. 异动告警

每条告警固定包含：

1. 时间。
2. 饰品名称、品质、平台。
3. 告警类型。
4. 变化前后数值。
5. 绝对变化和变化率。
6. 底价、最高求购价、在售、求购等关联数据。
7. 系统判断。
8. 近 3 次同类历史告警简报。
   1. 实施进度：推送消息已按在售、求购、底价、波动、成交分组展示近 3 次历史告警。

告警判断方向：

1. 偏买入机会。
2. 偏卖压风险。
3. 偏扫货拉升。
4. 偏流动性异常。
5. 横盘观察。
6. 暂不明确。

### 3. 告警降噪

告警触发不能只看固定阈值，需要同时考虑：

1. 绝对变化。
2. 相对变化率。
3. 历史波动基线。
4. 近 24 小时成交量。
5. 饰品流动性。
6. 同类告警冷却时间。
7. 重复告警合并。

## 五、UI 计划

整体设计为专业交易终端风格，信息密度高、少装饰、强对比，优先深色主题，适合长时间盯盘。

### 1. 页面

1. 监控总览页：展示自选饰品列表，支持按异动程度、涨跌幅、成交量、在售变化、求购变化排序。
   1. 实施进度：已增加饰品管理页，支持新增、编辑、停用、恢复监控饰品。
   2. 实施进度：监控总览已支持按状态分、底价、在售、求购、24h 成交、买入分和卖出分排序。
2. 饰品详情页：展示价格曲线、在售曲线、求购曲线、成交量柱状图、历史告警、挂单结构和时间段热力图。
   1. 实施进度：已增加单品详情时间段热力图和按类型聚合的历史告警简报。
3. 异动告警页：按时间倒序展示告警，支持按饰品、类型、严重程度、平台筛选。
   1. 实施进度：告警页已增加饰品、类型、严重度、平台筛选控件，后端告警 API 支持对应查询参数。
4. 机会榜页：分为买入机会榜、卖压风险榜、扫货拉升榜、异常波动榜。
   1. 实施进度：机会榜已按买入机会、卖压风险、扫货拉升、异常波动和观察候选分组展示。
   2. 实施进度：机会榜已接入同类告警 60 分钟回测信号，排序时参考历史胜率、平均变化和样本数。
5. 策略配置页：配置监控频率、变化阈值、推送冷却时间和不同监控池规则。
   1. 实施进度：默认策略已支持在前端编辑在售变化、求购变化、底价变化、价格波动异常、成交量异常、冷却时间和可信度最大降权。
6. 数据源状态页：展示采集成功率、接口延迟、最近失败、限流状态和任务运行情况。
   1. 实施进度：已增加采集运行日志，展示最近采集状态、耗时、快照数、告警数和错误数。
   2. 实施进度：已增加数据源质量指标，展示真实字段、补位字段、fallback 次数和最近错误。
   3. 实施进度：已在监控列表展示饰品最新快照可信度，并在数据源页列出低可信饰品。
   4. 实施进度：已增加运维健康接口，汇总采集成功率、推送成功率、worker 延迟、快照/告警数量和数据源异常计数。
   5. 实施进度：数据源状态页已展示运维健康、采集/推送成功率、worker 延迟、24h 快照、告警和异常数。
   6. 实施进度：数据源状态页已接入 `/api/retention`，展示行情快照、告警、回测和采集日志的保留策略、数量和最早记录时间。

### 2. 移动端

移动端优先展示告警流、机会榜和单品详情。表格改为紧凑卡片，复杂策略配置只保留核心开关和阈值。

### 3. 可视化

1. 价格曲线。
2. 在售数量曲线。
3. 求购数量曲线。
4. 成交量柱状图。
5. 买卖盘价差图。
   1. 实施进度：已增加买卖盘价差金额、价差率和饰品详情价差趋势图，监控总览支持价差率排序。
6. 时间段热力图。
   1. 实施进度：已在单品详情页按小时聚合展示在售、求购、成交和波动强度。
7. 历史告警时间线。
   1. 实施进度：已在饰品详情页增加历史告警时间线，按时间展示告警类型、方向、变化值和变化率。

## 六、开发计划

### 1. 技术架构

1. 前端：React + TypeScript + Vite / Next.js + Tailwind CSS。
2. 后端：Python FastAPI + SQLAlchemy。
3. 数据库：PostgreSQL，生产环境可启用 TimescaleDB。
4. 缓存：Redis。
5. 任务：APScheduler / Celery，按实际复杂度选择。
6. 数据分析：pandas / numpy。
7. 推送：Telegram Bot / 企业微信 Bot / 邮件。

### 2. 后端模块

1. 数据源接入模块。
   1. 实施进度：已增加可选 Steam 订单簿适配，配置 `item_nameid` 后可获取真实在售/求购深度。
   2. 实施进度：已增加饰品级 Steam NameID 管理，订单簿优先读取饰品配置。
   3. 实施进度：已增加 Steam NameID 自动发现服务和前端发现按钮。
   4. 实施进度：已增加 Steam 订单簿验证接口和前端验深度按钮。
   5. 实施进度：已增加 Steam NameID 批量发现和批量验证能力。
2. 饰品基础信息模块。
3. 行情快照采集模块。
4. 异动检测模块。
5. 告警记录模块。
6. 推送模块。
7. 综合评分模块。
8. 回测统计模块。
9. 策略配置模块。
10. 系统健康检查模块。

### 3. 数据设计

核心数据表包括：

1. 饰品表。
2. 平台表。
3. 监控池表。
4. 行情快照表。
5. 成交记录表。
6. 告警记录表。
7. 策略配置表。
8. 推送记录表。
9. 回测结果表。
10. 采集任务日志表。
   1. 实施进度：已实现采集运行日志表和查询接口。
11. 数据库迁移版本表。
   1. 实施进度：已增加 Alembic 基线迁移，后端容器启动前自动执行迁移。

设计要求：

1. 饰品 ID 必须稳定。
2. 平台与饰品属性分离。
3. 原始数据和计算结果分离。
4. 快照数据和告警数据分离。
5. 采集失败必须记录。
6. 数据源字段可信度必须可追踪，区分真实返回字段和补位字段。
7. 告警必须能追溯到触发时的行情快照。

### 4. API 范围

1. 获取监控列表。
2. 获取单品详情。
3. 获取价格历史。
   1. 实施进度：已增加单品价格历史接口。
4. 获取在售历史。
   1. 实施进度：已增加单品在售历史接口。
5. 获取求购历史。
   1. 实施进度：已增加单品求购历史接口。
6. 获取告警列表。
7. 获取机会榜。
8. 管理监控池。
   1. 实施进度：已完成饰品级管理接口和监控池分组归属。
   2. 实施进度：已增加监控池新增和编辑 API，支持维护名称、采集间隔和说明。
9. 更新策略配置。
10. 获取数据源状态。

## 七、实施部署计划

云服务器已购买，后续直接部署到该服务器。

### 1. 部署结构

使用 Docker Compose 部署，建议拆分为：

1. frontend。
2. backend。
3. worker。
4. postgres / timescaledb。
5. redis。

### 2. 上线顺序

1. 初始化服务器环境。
2. 配置 Docker 和 Docker Compose。
   1. 实施进度：已增强 Docker Compose 健康检查、重启策略和环境变量模板。
3. 部署数据库与 Redis。
   1. 实施进度：已增加数据库迁移启动器，支持全新数据库初始化和早期 MVP 表结构纳入迁移版本管理。
4. 部署后端 API。
5. 部署采集与告警 worker。
6. 部署前端监控盘。
7. 配置推送渠道。
8. 观察至少一轮完整采集周期。
   1. 实施进度：已增加 `scripts/verify_deploy.ps1`，支持部署后验证和可选触发采集。
   2. 实施进度：部署验证脚本已读取 `/api/ops/health`，用于确认采集、推送和 worker 运行状态。
9. 部署前备份数据库。
   1. 实施进度：已增加 `scripts/backup_postgres.ps1`，支持 Docker Compose PostgreSQL 生成 `.dump` 备份并清理过期备份。

### 3. 运维监控

需要持续关注：

1. 采集成功率。
2. API 响应时间。
3. 数据库写入量。
4. 推送成功率。
5. 告警数量。
6. CPU / 内存 / 磁盘。
7. 数据源异常次数。
8. worker 是否正常运行。
   1. 实施进度：已增加 `/api/ops/health`，按最近 24 小时输出采集、推送、worker 延迟和数据源异常指标。
   2. 实施进度：前端数据源状态页已接入 `/api/ops/health`，可直接查看 worker 延迟和整体运维状态。

### 4. 备份与安全

1. 数据库每日备份。
   1. 实施进度：已增加 PostgreSQL 备份脚本，后续云服务器部署时可接入 cron/systemd timer 做每日备份。
2. 重要配置单独备份。
   1. 实施进度：已增加 `scripts/backup_config.ps1`，支持打包 `.env`、Compose、项目说明和部署脚本，并清理过期配置备份。
3. 原始行情至少保存 3-6 个月。
   1. 实施进度：已增加 `/api/retention`，展示行情快照 180 天保留策略、当前数量和最早记录时间。
   2. 实施进度：数据源状态页已展示行情快照保留策略，便于部署后巡检数据留存情况。
   3. 实施进度：已增加 `/api/retention/cleanup`，可清理 180 天前且未被告警引用的行情快照。
4. 告警和回测数据长期保存。
   1. 实施进度：已增加 `/api/retention`，展示告警和回测结果长期保留策略、当前数量和最早记录时间。
   2. 实施进度：数据源状态页已展示告警、回测和采集日志保留策略，便于上线后复盘与运维检查。
   3. 实施进度：数据保留清理不会删除告警、回测结果或被告警引用的行情快照，保证复盘链路可追溯。
5. 后台登录鉴权。
6. API 访问鉴权。
7. 管理接口限制 IP 或增加访问控制。
8. Token、API Key、数据库密码全部使用环境变量。

## 八、阶段里程碑

### P0：可行性验证

1. 确认数据源可用性。
2. 选定 30 个测试饰品。
   1. 实施进度：已扩充默认 seed 到 30 个测试饰品，其中超导体、清凉薄荷在重点池，其余武器、手套、箱子和贴纸在观察池。
3. 连续采集 7 天。
4. 验证底价、在售、求购、成交数据准确性。
5. 输出第一版异动告警。

### P1：MVP 平台

1. 扩展到 100-300 个饰品。
2. 上线监控总览页。
3. 上线饰品详情页。
4. 上线异动告警页。
5. 上线推送模块。
6. 上线历史告警简报。

### P2：决策增强

1. 加入综合评分。
2. 加入机会榜。
3. 加入时间段热力图。
4. 加入告警回测。
   1. 实施进度：已增加告警回测结果表、回测 API 和前端回测页。
5. 加入策略配置。

### P3：多平台价差

1. 接入第二、第三市场。
2. 计算跨平台价差。
3. 加入手续费、提现费、汇率。
   1. 实施进度：已在策略配置中加入卖出手续费、提现费率和汇率/折算系数。
4. 输出真实净利润空间。
   1. 实施进度：已基于当前单平台底价和最高求购价输出扣费后净卖价、净价差和净价差率，后续接多平台后可扩展为跨平台净利润空间。

### P4：量化盘稳定期

1. 建立长期回测。
   1. 实施进度：已建立多窗口回测记录，后续可扩展长期统计和策略调参。
   2. 实施进度：已增加回测最大上涨、最大回撤、盈亏比和样本置信等级，回测页可查看更完整的长期统计口径。
2. 调整阈值模型。
   1. 实施进度：已在机会榜排序中加入同类告警回测修正，作为策略调参前的历史表现参考。
   2. 实施进度：已增加基于 60 分钟回测窗口的调参建议，按样本量、胜率和平均变化提示继续观察、收紧阈值、适度放宽或保持当前。
3. 建立品类策略。
   1. 实施进度：已增加品类策略画像，按手套、武器、箱子/贴纸等分类输出买入/卖出分修正和解释理由。
4. 形成买入、持有、卖出、观望四类参考信号。
   1. 实施进度：已完成四类参考信号第一版，并在监控总览、饰品详情和机会卡片展示信号、置信度与依据。

## 九、上线前验收标准

1. 数据采集连续稳定运行 7 天。
2. 重点饰品价格数据准确。
3. 在售变化与实际市场基本一致。
4. 求购变化与实际市场基本一致。
5. 告警不会频繁重复刷屏。
6. 推送延迟可接受。
7. 单品详情图表可正常查看。
8. 机会榜排序逻辑可解释。
9. 每条告警都能追溯原始数据。
10. 服务器重启后服务能自动恢复。
11. 数据库有备份。
12. 策略参数可以调整。
13. 出现数据源异常时不会批量误推大量告警。
    1. 实施进度：已增加采集质量熔断，异常采集轮次仍记录告警和 skipped 推送原因，但不触发外部 Webhook。
14. 历史告警能用于复盘。
15. 平台输出参考信号，不直接触发自动买卖。

## 十、实施前确认项

正式编码前需要确认：

1. 首选数据源。 
   1. 答复：这个我不清楚，steam能直接获取吗？
   2. 实施选择：MVP 先实现 Steam `priceoverview` 基础适配层，并保留 Mock 数据源用于本地自测；Steam 可直接获取部分价格数据，但在售数量、求购深度等完整买卖盘数据需要后续接更完整的市场数据源。
2. 第一批监控饰品清单。
   1. 答复：超导体、清凉薄荷
3. 首选推送渠道。
   1. 答复：微信或QQ
   2. 实施选择：已实现微信/QQ Webhook 推送适配层；未配置 Webhook 时记录 skipped，不阻断采集链路。
4. 云服务器系统版本和登录方式。
   1. 答复：opencloud os 9。账密我会在后续发给你
5. 是否优先使用 PostgreSQL 还是 TimescaleDB。
   1. 答复：这个我不清楚，你从架构师的角度去选择
   2. 实施选择：优先 PostgreSQL，行情快照表结构保持 TimescaleDB 扩展友好。
6. 前端技术选型使用 Vite 还是 Next.js。
   1. 答复：这个我不清楚，你从架构师的角度去选择
   2. 实施选择：优先 Vite + React + TypeScript，降低私有监控盘的部署复杂度。
7. MVP 是否先做登录鉴权。
   1. 答复：暂时不做
8. 补充：github仓库已创建：git@github.com:SmallThreeStone/CS-Quantification.git。你写到claude.md里
